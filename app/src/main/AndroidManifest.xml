<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.ijobs">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activities.chat.ListMessagesActivity"></activity>
        <activity android:name=".activities.chat.SendMessageActivity" />
        <activity android:name=".activities.jobRecruiter.JobRecruiterSeekerDetailsActivity" />
        <activity android:name=".activities.jobSeeker.JobSeekerJobDetailsActivity" />
        <activity android:name=".activities.jobRecruiter.CreateJobRecruiterAdActivity" />
        <activity android:name=".activities.jobRecruiter.JobRecruiterListingScreen" />
        <activity android:name=".activities.jobSeeker.JobSeekerListingScreen" />
        <activity android:name=".activities.jobSeeker.CreateJobSeekerProfileActivity" />
        <activity android:name=".CreateUserProfileActivity" />
        <activity android:name=".MainActivity" />
        <activity android:name=".AuthActivity" />
        <activity android:name=".IntroActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="com.facebook.sdk.ApplicationId"
            android:value="@string/facebook_app_id" />

    </application>

</manifest>